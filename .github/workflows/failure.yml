name: Failure Handler Workflow

on:
  repository_dispatch:
    types: [handle_failure]

jobs:
  handle_failure:
    runs-on: ubuntu-latest
    steps:
      - name: Notify Failure
        run: |
          echo "Main workflow failed! Notify appropriate channels."
